## Qué es HTTP

#### HTTP es un protocolo de la capa de "aplicación" del modelo OSI. Define cómo un cliente solicita recursos y cómo un servidor le responde, utilizando métodos como GET, POST, PUT, etc. Es un protocolo donde cada petición es independiente, enviando datos en mensajes estructurados con headers y body.