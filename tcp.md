## Qué es TCP

#### TCP es un protocolo de la capa de "transporte" del modelo OSI. Establece una conexión confiable entre dos extremos, divide los datos en paquetes, los cuales contienen un header con el orden de cómo "ensamblarlos" y chequeos de errores por si alguno no llega correctamente a destino y debe ser retransmitido.